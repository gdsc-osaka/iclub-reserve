/**
 * メディアストレージ（R2）内の配置場所（キー接頭辞）の一覧。
 *
 * 【バケット設計（ADR-005）】
 * アプリが保存する画像全般（施設写真、今後の団体ロゴやユーザーアイコン等）は
 * 環境ごとに 1 つのバケットに集約し、キーの接頭辞で種類を分ける。
 *
 * 種類を足すときはここに行を足し、接頭辞が重ならないようにする。
 * なお、接頭辞は権限の境界ではない（Worker のバインディングはバケット全体を読み書きできる）。
 * 誰に見せるかは、種類ごとの配信ルート（例: `/facility-photos/:photoName`）で決める。
 */
export const MediaPrefix = {
  /** 施設・設備写真の保存場所 */
  FacilityPhotos: "facilities/",
} as const;
